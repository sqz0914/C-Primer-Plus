#include<stdio.h>
#include<math.h>
#define PI 3.1415926
int main(void)
{
	double num;
	
	printf("What is the value of pi?\n");
	scanf("%lf",&num);
	
	while(fabs(num - PI) > 0.0001)
	{
		printf("Try again!\n");
		scanf("%lf",&num);
	}
	printf("Close enough!\n");
	return 0;
}
